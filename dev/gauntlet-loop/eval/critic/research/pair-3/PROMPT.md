The bar: one paragraph on whether code review catches bugs, grounded in the single source provided.
On each side: read `para.md` in `a/` and `b/` and that side's `sources/paper.txt`; check every figure against the source.
Reading floors: every figure matches the source; the source's own caveat is carried.
