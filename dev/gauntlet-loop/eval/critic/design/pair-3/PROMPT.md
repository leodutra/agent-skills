The bar: an empty state for a list with no items: one line saying what belongs here, one action to add the first item.
On each side: open `a/empty.svg` and `b/empty.svg` as text; read the copy, sizes and colours.
Reading floors: text passes 4.5:1 on its background; exactly one primary action.
