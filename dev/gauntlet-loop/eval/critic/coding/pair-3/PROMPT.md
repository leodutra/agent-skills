The bar: a `clamp(n, lo, hi)` function that returns n limited to the range, and rejects a range where lo is greater than hi by naming both bounds.
On each side: read `a/clamp.mjs` and `b/clamp.mjs`; run `node try.mjs a` and `node try.mjs b`.
Reading floors: a thrown error names the bad input.
