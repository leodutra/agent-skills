The bar: the text a user sees when a file upload fails because the file is too large; it must say what happened and exactly what to do next.
On each side: read `a/text.md` and `b/text.md`; list what the user learns from each and what they can do next.
Reading floors: states the limit as a number; gives one concrete next step.
