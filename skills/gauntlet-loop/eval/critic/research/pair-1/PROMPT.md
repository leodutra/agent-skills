The bar: a short answer to "does a four-day work week reduce output?", where every claim is backed by a source that says what is claimed.
On each side: read `answer.md` in `a/` and in `b/`, then open every file it cites under that side's `sources/` and check the source says what the answer claims.
Reading floors: every citation opens and says what is claimed; no claim is stronger than its source.
