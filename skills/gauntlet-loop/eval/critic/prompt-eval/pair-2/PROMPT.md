The bar: a system prompt that classifies a support ticket into exactly one of four labels as JSON; judged on the frozen harness results beside each prompt.
On each side: read `prompt.txt` and `results.json` in `a/` and `b/`; compare pass rates and schema-invalid counts.
Reading floors: zero schema-invalid outputs; the label set is stated in the prompt.
