The bar: a system prompt that rewrites a commit message to the imperative mood in at most 72 characters; judged on the frozen harness results beside each prompt.
On each side: read `prompt.txt` and `results.json` in `a/` and `b/`; compare pass rates; the run-to-run spread is one case.
Reading floors: zero schema-invalid outputs.
