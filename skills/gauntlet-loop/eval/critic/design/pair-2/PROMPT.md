The bar: a text button drawn in two states, default and keyboard focus; the focus state must be visibly different from the default.
On each side: open `default.svg` and `focus.svg` in `a/` and in `b/` as text and diff them; say what changes between the two states.
Reading floors: the focus state differs from the default; the label passes 4.5:1 against the button fill.
