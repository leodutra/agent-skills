The bar: a pricing card for a "Pro" plan: the price is the first thing the eye lands on, body text passes 4.5:1 contrast, one clear primary action.
On each side: open `a/card.svg` and `b/card.svg` as text; read font sizes, weights and fill colours; compute the contrast ratio of the body text against the card background from the hex pairs.
Reading floors: body text contrast is at least 4.5:1; the primary action is distinguishable from the card.
